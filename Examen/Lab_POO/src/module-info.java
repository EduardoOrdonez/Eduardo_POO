/**
 * 
 */
/**
 * @author ESPE
 *
 */
module Lab_POO {
	int [] nums=new int [6];
	int n;
	n=(int) (Math.random()*49+1);
	//si no esta repetido lo guardamos en el array
	 if(!repetido(n. nums, genereados)) {
		nums[generados]=n;
		generados++;
		
	 }
	
}